<?xml version="1.0" encoding="utf-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:WATPlanMobile.Models;assembly=WATPlanMobile"
    x:Class="WATPlanMobile.Pages.PlanPage"
    x:DataType="models:PlanModel"
    Title="{Binding Name}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Zapisz" Order="Primary" Clicked="Zapisz"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <RefreshView Refreshing="RefreshView_OnRefreshing" x:Name="RefreshView">
            <StackLayout>
                <Label Text="{Binding Name, StringFormat='Tu będzie wyświetlony plan z API: {0}'}"
                       VerticalOptions="CenterAndExpand"
                       HorizontalOptions="CenterAndExpand"
                       FontSize="Title"/>
                <Label Text="{Binding Events.Count, StringFormat='Liczba wydarzeń: {0}'}"
                       VerticalOptions="CenterAndExpand"
                       HorizontalOptions="CenterAndExpand"/>
            </StackLayout>
        </RefreshView>
    </ContentPage.Content>
</ContentPage>